# 环境

> 后端 jdk17 + maven3.9.12 + mysql8
> 前端 node22.21.1

> 后端数据库配置：

1. 将 `backend/src/main/resources` 里的 `application-local.yml.example` 改成 `application-local.yml`
2. 修改数据库地址、端口、账号密码
3. 或者参照 `backend/src/main/resources/application-local.yml` 里的变量名称设置环境变量

> 数据库脚本: `sql/init.sql`
